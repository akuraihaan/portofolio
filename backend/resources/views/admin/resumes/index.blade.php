<x-admin-layout title="Resumes · bworiey">
    <div class="mb-8"><p class="text-sm text-[#7c7c6f]">{ Private file library }</p><h1 class="mt-2 font-display text-5xl font-semibold tracking-[-.08em]">Resumes.</h1><p class="mt-3 max-w-2xl text-sm leading-6 text-[#7c7c6f]">CV disimpan di storage privat dan hanya dapat diunduh melalui route publik yang memeriksa status publikasi.</p></div>
    @if ($errors->any())<div class="mb-6 rounded-lg border border-red-400/40 bg-red-400/10 px-4 py-3 text-sm text-red-200"><ul class="list-disc space-y-1 pl-5">@foreach ($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
    <section class="mb-6 rounded-lg border border-[#42433d] bg-[#191919] p-5 sm:p-7"><h2 class="font-display text-2xl">Upload CV</h2><form method="POST" action="{{ route('admin.resumes.store') }}" enctype="multipart/form-data" class="mt-5 grid gap-4 md:grid-cols-2">@csrf<input name="title" placeholder="Judul CV" required class="admin-input"><input name="version" placeholder="Versi (opsional)" class="admin-input"><select name="language" class="admin-input"><option value="id">Indonesia</option><option value="en">English</option></select><input type="file" name="file" accept="application/pdf" required class="admin-input p-2"><label class="block text-sm text-[#7c7c6f]">Terbit pada<input type="datetime-local" name="published_at" class="admin-input mt-2"></label><label class="flex items-center gap-3 self-end text-sm"><input type="hidden" name="is_active" value="0"><input type="checkbox" name="is_active" value="1" checked class="h-4 w-4 accent-[#abff84]">Aktifkan</label><div class="md:col-span-2"><button class="rounded-full bg-[#abff84] px-5 py-2.5 text-sm font-semibold text-[#0e100f]">Upload CV</button></div></form></section>
    <section class="space-y-4">@forelse ($resumes as $resume)<article class="flex flex-col gap-4 rounded-lg border border-[#42433d] bg-[#191919] p-5 sm:flex-row sm:items-center sm:justify-between"><div><p class="font-medium">{{ $resume->title }} <span class="ml-2 rounded-full border border-[#42433d] px-2 py-1 text-xs text-[#7c7c6f]">{{ strtoupper($resume->language) }}</span></p><p class="mt-2 text-sm text-[#7c7c6f]">{{ $resume->version ?: 'Tanpa versi' }} · {{ number_format($resume->download_count) }} download · {{ $resume->is_active ? 'Aktif' : 'Nonaktif' }}</p></div><form method="POST" action="{{ route('admin.resumes.destroy', $resume) }}" onsubmit="return confirm('Hapus file CV ini?')">@csrf @method('DELETE')<button class="rounded-full border border-red-300/50 px-5 py-2.5 text-sm text-red-200">Hapus</button></form></article>@empty<div class="rounded-lg border border-dashed border-[#42433d] p-8 text-center text-sm text-[#7c7c6f]">Belum ada CV yang dipublikasikan.</div>@endforelse</section>
</x-admin-layout>
