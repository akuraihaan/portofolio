<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">{{ __('Account security') }}</h2></x-slot>
    <div class="py-12"><div class="max-w-7xl mx-auto space-y-6 sm:px-6 lg:px-8">
        @if (session('status')) <div class="rounded-md bg-green-50 p-4 text-sm text-green-700">{{ session('status') }}</div> @endif
        <div class="bg-white p-6 shadow-sm sm:rounded-lg"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h3 class="text-lg font-medium text-gray-900">Active sessions</h3><p class="mt-1 text-sm text-gray-600">Session IDs are never shown. IP addresses are masked in this view.</p></div><form method="POST" action="{{ route('profile.security.other-sessions') }}" class="flex gap-2">@csrf @method('DELETE')<input type="password" name="password" placeholder="Confirm password" required class="rounded-md border-gray-300 text-sm"><button class="rounded-md bg-gray-900 px-4 py-2 text-sm font-medium text-white">Log out other devices</button></form></div><div class="mt-6 divide-y divide-gray-200">@forelse($sessions as $session)<div class="flex flex-col justify-between gap-3 py-4 sm:flex-row sm:items-center"><div><p class="text-sm font-medium text-gray-900">{{ str_contains((string)$session->user_agent, 'Mobile') ? 'Mobile device' : 'Desktop device' }} @if($session->id === $currentSession)<span class="ml-2 rounded-full bg-green-100 px-2 py-1 text-xs text-green-700">This device</span>@endif</p><p class="mt-1 text-xs text-gray-500">{{ $session->ip_address ? preg_replace('/\.\d+$/', '.•••', $session->ip_address) : 'Unknown IP' }} · Active {{ IlluminateSupportCarbon::createFromTimestamp($session->last_activity)->diffForHumans() }}</p></div>@if($session->id !== $currentSession)<form method="POST" action="{{ route('profile.security.destroy', $session->id) }}">@csrf @method('DELETE')<button class="text-sm text-red-600 hover:underline">Log out</button></form>@endif</div>@empty<p class="py-8 text-sm text-gray-500">No database-backed sessions found.</p>@endforelse</div></div>
    </div></div>
</x-app-layout>
